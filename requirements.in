# Runtime dependencies
azure-functions
azure-storage-blob
azure-identity
opencv-python
numpy
Pillow
huggingface-hub
torch
transformers
# timm provides the ResNet-50 backbone for the DETR detector (use_timm_backbone=True);
# it is a runtime dependency even though nothing imports it directly.
timm

# Dev/test dependencies
pytest
pytest-cov
ruff
mypy
